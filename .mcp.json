{
  "mcpServers": {
    "mailchimp": {
      "command": "uvx",
      "args": ["--with", "mcp<2", "mailchimp-mcp"],
      "env": {
        "MAILCHIMP_API_KEY": "${MAILCHIMP_API_KEY}"
      }
    }
  }
}
